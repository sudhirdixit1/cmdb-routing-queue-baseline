\section{Case study: a configuration management database}
\label{sec:cmdb}

A configuration management database is the register an IT estate keeps of the
items it contains, and it is bought partly on the argument that operational
analytics need it. This section evaluates one against a named decision on a
public log: will this case be reassigned to another group before it is
resolved? The estate is a bank's and the log is BPI Challenge 2014
\citep{vandongen2014bpic}; the register field is the affected configuration
item, with \cardF\ distinct values. It is reported at greater length than the
other \nPairs\ pairs because it is the case that motivated the study and the
one on which the decision time --- when in the service-desk process the
prediction is made --- decides the answer.

\subsection{One log, two answers, and what separates them}
\label{sec:cohort}


This log appears twice in this paper. Section~\ref{sec:multilog} applies the
registered generic rules of Section~\ref{sec:design}, which admit
\nRegisteredCohort\ cases and define the target by the registered handover
rule. This section uses the cohort and the reassignment target defined for the
estate: \nCohort\ cases at prevalence \casePrevalence\ (\casePrevalenceTest\
in the test half), \nCaseTrain\ in
training and \nCaseTest\ in test. The first set is a strict subset of the
second; the extra \nExtraCases\ cases are those the estate's own cutoff
excludes.

The two produce different answers about the register at the later decision
time, and the difference is large enough to change the sign. Against intake
plus group plus the knowledge reference, the estate's cohort and reassignment
target give \VTtwoKnow\ and the registered cohort and handover target give
\VregisteredTauTwo, on which the planned contrast \textsf{PC3}'s one-sided
$p$-value rejects at the Holm-adjusted level while its interval does not
exclude zero (Section~\ref{sec:planned}). Table~\ref{tab:cohort} takes the
manuscript from the first to the second one factor at a time.

\textbf{It is the target, not the cohort.} Over the cohort $\times$ target
design the target carries \shareTargetPct\ of the variance in the increment,
the cohort \shareCohortPct, and their interaction \shareCohortTargetPct.
Reassignment to another group before resolution has prevalence
\casePrevalence; handover between activity groups, under the registered rule,
has prevalence \prevRegisteredHandover. They are not the same question and the
register is not worth the same to them. Two further candidate explanations
were checked and neither is one: the two analyses reach the same attribute
values on every row, so it is not a data-preparation difference; and they run
the same pipeline at the same reference cell, so it is not a modelling
difference.

\textbf{An axis nobody declares, and this paper was standing on it.} Both
analyses sort the log in time and split at seventy per cent of the row order,
and \nTiedTimestamps\ incidents share their timestamp with another; the sort
this estate's analysis first used was not stable, so its tie order was
neither declared nor reproducible, and it printed \VpublishedTauTwo\ where the
two declared rules both give \VTtwoKnow. That first number was never
published and is not cited here as though it were: it is recomputed in the
archive from that analysis's own loader
(\texttt{scripts/s32\_cohort.py}, \textsf{published\_cell}), so the
reconciliation below anchors on the object rather than on a recollection of
it. Over \nTiebreakDraws\ random tie-breaks
the increment ranges over \tiebreakRange\ AUC without changing sign
(\nTiebreakPositive\ of \nTiebreakDraws\ draws positive), so no conclusion
turns on it --- but it is a quarter of the point estimate's own magnitude,
produced by a choice no reporting standard asks anyone to state, which is why
Section~\ref{sec:standard} now asks. Every number in this section is computed
under a declared tie-break, by incident identifier, on the estate's cohort and
reassignment target; the registered cohort's answers are in
Section~\ref{sec:multilog} with the other \nPairs\ pairs, so the decision-time
result below is a statement about a routing decision on this estate and not
about handover in general.

\subsection{Three decision times}
\label{sec:twodecision}

A decision time fixes three things at once --- which cases are in scope, what
is known about them, and which fields are admissible --- and the estate's
surface is estimated at each of three moments.

\input{tables/tau}

\textbf{The register's worth turns on which one the analyst occupies}: at
$\tau_0$ it is \VtauZero\ [\VtauZeroLo, \VtauZeroHi] over \nTauZero\
\emph{interactions} --- calls to the desk, of which only a minority ever
become incidents, which is why that population is far larger than the
\nTauTwo\ incidents the later moments carry --- at
$\tau_1$ \VtauOne\ [\VtauOneLo, \VtauOneHi], and at $\tau_2$ against the
intake block plus the group it is \VTtwoGroup\ [\VTtwoGroupLo,
\VTtwoGroupHi] while against the block, the group and the knowledge reference
it is \VTtwoKnow\ [\VTtwoKnowLo, \VTtwoKnowHi] --- not resolvably different
from zero. \textbf{The step from $\tau_1$ to $\tau_2$ is an information step
only on the matched population}: \nTauTwoUnmatched\ of the \nTauTwo\
incidents have no interaction record, so the \tauMatchedRow\ row of
Table~\ref{tab:tau} restricts $\tau_2$ to $\tau_1$'s own cases and it is that
row, not the \tauPlainRow, that isolates the information.
Supplement~\ref{app:taudetail} gives each moment's population, register and
admissible set.

\subsection{The leakage tipping point, and register quality}
\label{sec:quality}

Two further experiments on this estate are in Supplement~\ref{app:casedetail}
and summarised here.

\textbf{A field that looks like leakage has a tipping point, and where it
sits is what a reader has to judge.} The knowledge reference is the field
that takes the register's increment to indistinguishable from zero, and
whether it was available at $\tau_2$ is probable but unproven. Writing
$\lambda$ for the share of its values assumed written \emph{after} the
incident --- so that a larger $\lambda$ makes more of the field inadmissible
--- the register is worth \VTtwoKnow\ [\VTtwoKnowLo, \VTtwoKnowHi] at
$\lambda = 0$, not resolvably different from zero, and reaches half of its
knowledge-free value at $\lambda = \lambdaHalf$. A reader who believes more
than \lambdaHalfPct\ of references are written after the incident should read
this estate's register as worth at least half of \VTtwoGroup\ AUC, and one
who believes almost none are should read it as worth almost nothing. We
cannot measure $\lambda$, so the disposition of an unprovable admissibility
assumption is reported as a sensitivity analysis and not as a headline
(Figure~\ref{fig:tipping}, Table~\ref{tab:tipping}).

\textbf{Register quality is an axis, not a caveat, and which rows go missing
matters as much as how many.} Under \nQualityKinds\ declared degradation
mechanisms the increment at the reference cell moves from \qClean\ on a clean
register to \qRareHalf\ when half the population is lost from its long tail
and \qCommonHalf\ when half is lost from its core. Three of the mechanisms
are \emph{dependent} --- on time, on content, and on a training-estimated
propensity that correlates missingness with the outcome --- and they carry
severity integrals \integralTime, \integralContent\ and
\integralPropensity\ against \integralMaskRare\ for the independent long-tail
mechanism, which they bracket rather than track. A quality axis built only
from independent coin flips measures one corner of the question.

\textbf{Which halves are degraded, exactly.} A degraded register is a state of
the world and not a training accident, so \textbf{the mechanism is applied to
both halves of the data} while its \emph{parameters} are estimated from the
training half alone, and the repository executes that property rather than
asserting it. Supplement~\ref{app:degrade} gives the construction, the
executed check and why a mechanism applied to training alone would measure a
different thing; Tables~\ref{tab:quality2} and~\ref{tab:quality3} carry every
mechanism and level.

\subsection{What this says to a configuration management programme}
\label{sec:programme}

This subsection is the reading a service-management audience will want, stated
in the estate's own vocabulary. It is a reading of \emph{one} estate on
\emph{one} public log and it is offered as one; Section~\ref{sec:lim} says
what a second would need.

\paragraph{Which field is which}
The prediction is: \emph{will this case be reassigned to another group before
it is resolved?} The \textbf{register} $f$ is the affected configuration
item, \cardF\ distinct values. Its \textbf{coarsenings} are the item's type
(\nLayerTypeLevels\ values) and subtype (\nLayerSubtypeLevels), each given its
own role rather than swept into the baseline. The \textbf{free field} $g$ is
the assignment group --- the routing queue, which is recorded without a
configuration management database and is the field the register has to beat.
The \textbf{intake block} $B_0$ is what the desk records when the case
arrives: category, impact, urgency and priority. The \textbf{knowledge
reference} is a pointer to a knowledge-base article, admitted as a further
rung because whether it is available at the moment of decision is exactly the
question Section~\ref{sec:quality} cannot settle.

\paragraph{The three moments, in service-desk terms}
$\tau_0$ is \emph{an interaction is logged}: somebody has contacted the desk,
every call is in scope, and \nTauZero\ of them are.
$\tau_1$ is the same moment restricted to the calls that will escalate ---
the same information, a different population. $\tau_2$ is \emph{an incident
record is created}: \nTauTwo\ cases, more known about each, and
\nTauTwoUnmatched\ of them with no interaction record at all.
Table~\ref{tab:tau} is the ladder. \textbf{Read down it and the register's
worth falls from \VtauZero\ AUC to \VtauOne\ to \VTtwoGroup\ once the routing
queue is in the baseline, and to \VTtwoKnow\ --- not resolvably different from
zero --- once the knowledge reference is admitted too.} A programme that
justifies the register on a first-touch number and then deploys the model at
incident creation is quoting the top of a ladder and standing on the bottom of
it; Section~\ref{sec:nbregret} prices that in cases per thousand:
\promisedPerThousand\ true positives promised per thousand cases against
\deliveredPerThousand\ delivered.

\paragraph{Which layer of the register pays}
A programme does not choose whether to have a configuration management
database; it chooses how much of one to populate and maintain, and that is a
choice among \emph{layers}. On the \emph{registered} cohort of
Section~\ref{sec:design} --- \layerLadderN\ cases, one-hot logistic
regression at the fixed temporal holdout, against the intake block at base AUC
\layerBaseHandover\ --- the item's \emph{type} is worth \layerTypeHandover\
AUC over intake, its \emph{subtype} \layerSubtypeHandover, and the item itself
\layerItemHandover; with the subtype already in the model the item still adds
\layerItemMarginalHandover. \textbf{That ladder is not
Table~\ref{tab:tau}'s cell and does not reconcile with it arithmetically},
because the table is on the estate's own cohort and its reassignment target
--- the pair Section~\ref{sec:cohort} shows disagree about this register ---
so the increment over intake is \layerItemHandover\ here and \VtauTwo\ there
and neither is the other one mismeasured (Supplement~\ref{app:layers}).
\textbf{On this estate and this question the coarse layers are not free, and
the finest layer is not redundant}: \layerTypeSharePctHandover\ of what the
item is worth over the intake block is already reachable from a thirteen-value
type field, and the item still adds \layerItemMarginalHandover\ once the
subtype is in the model. On the duration question the same ladder gives
\layerTypeDuration, \layerSubtypeDuration\ and \layerItemDuration, with
\layerItemMarginalDuration\ marginal, and the type reaches
\layerTypeSharePctDuration. \textbf{So the split between the layers is a
property of the target and not of the register}, and
Supplement~\ref{app:layers} carries two further logs on which every coarse
layer is worth essentially nothing while the finest one is not. It is not a
procurement conclusion: nothing here prices what a layer costs to populate or
to keep accurate, and Section~\ref{sec:lim} says so.

\paragraph{What would have to change for the answer to move}
Three things, in the order a programme could act on them. \textbf{A
timestamped field history.} The knowledge reference decides this estate's
answer and its admissibility is settled here by probability rather than by
evidence; a register that records when each field value was written turns
Section~\ref{sec:quality}'s tipping-point analysis into a measurement.
\textbf{A complete population at the decision time.} \nTauTwoUnmatched\ of the
incidents have no interaction record, and the $\tau_1$-to-$\tau_2$ step is an
information step only on the cases that have both; a desk that can identify
its own cases across the two systems recovers that comparison. \textbf{A
target the desk actually acts on.} The register's sign changes with the
target on this log (Section~\ref{sec:cohort}), and reassignment is the
question this desk routes on; a programme evaluating a register against a
generic handover rule is answering a question nobody asked it.

\subsection{The absorption, and why the ratio is secondary}
\label{sec:absorption}

The quantity a report of this kind usually leads with is the \emph{relative}
reduction $R$: the share of the register's apparent value that a single free
field absorbs. The absolute absorption $D = V(f \mid B_0) - V(f \mid B_1)$ is
reported first, with its own interval, and $R$ second with a Fieller set and
the set's kind. On this estate at the reference cell $D$ is \Dabs\ [\DabsLo,
\DabsHi] and $R$ is \Rref\ with a \RrefKind\ Fieller set. This ladder is the
favourable case: \nFiellerUnboundedCase\ of the \nFiellerCase\ reductions it
produces have a Fieller set that is not a bounded interval, because the
denominator is resolvably positive at every rung of it. Across the corpus that
is not so --- \nFiellerUnbounded\ of \nFieller\ reductions have a non-bounded
set, and a percentile interval reports a bounded one for every one of them ---
which is why the ratio is secondary everywhere and not only here. The full
ladder is Table~\ref{tab:absorption}.
