HIGHLIGHTS

Elsevier allows three to five bullets, each at most 85 characters including
spaces. These are the five, with their lengths, so a future edit can check the
limit without re-counting by hand.

**Do not count these by hand.**  `python scripts/check_highlights.py` derives
every length below and fails if one is wrong or over the limit; `--fix`
rewrites them.  The file has now shipped twice with hand-counted lengths that
were wrong -- round sixteen's five were wrong by 3 to 11 characters, and round
eighteen's rewrite got four of nine wrong on the first attempt, in a file whose
own preamble warned against exactly that.  A count a human writes is a claim
like any other and belongs in a script.

[72] A field's incremental predictive value is a surface, not a single number
[72] A variance decomposition says which analyst choice moves the answer most
[74] Simultaneous bands label a surface beneficial, sign-changing or unresolved
[74] One-number reporting misstates the sign for many admissible specifications
[71] A register's worth depends on which decision time the analyst stands at


-- paste-ready, without the length prefixes --

A field's incremental predictive value is a surface, not a single number
A variance decomposition says which analyst choice moves the answer most
Simultaneous bands label a surface beneficial, sign-changing or unresolved
One-number reporting misstates the sign for many admissible specifications
A register's worth depends on which decision time the analyst stands at
